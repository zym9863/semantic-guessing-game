[project]
name = "semantic-guessing-game"
version = "0.1.0"
description = "基于Word2Vec的中文语义猜词游戏"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "gensim>=4.3.0",
    "numpy>=1.24.0",
]

[project.scripts]
start = "app:main"
